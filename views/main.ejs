<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Quora</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h1>Quora posts</h1>
    <%for(let post of posts) {%>
    <div>
      <h2>@ Username : <%= post.username%></h2>
      <h3>Content : <%= post.content %></h3>
      <a href="/posts/<%=post.id%>">Click to see in detail</a>
      <button
        id="edit"
        onclick="window.location.href='/posts/<%=post.id%>/edit'"
      >
        Edit
      </button>
      <br />
      <form action="/posts/<%= post.id%>?_method=DELETE" method="post">
        <button id="edit">Delete</button>
      </form>
    </div>

    <%} %>

    <button id="add" onclick="window.location.href='/posts/new'">
      Click to add
    </button>
  </body>
</html>
